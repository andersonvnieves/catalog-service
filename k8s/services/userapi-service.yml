apiVersion: v1
kind: Service
metadata:
  name: catalogapi-service
  namespace: fgc
spec:
  selector:
    app: catalogapi
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30081
  type: NodePort